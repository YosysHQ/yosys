pattern ice40_wrapcarry

match carry
	select carry->type.in(\SB_CARRY)
endmatch

match lut
	select lut->type.in(\SB_LUT4)
	index <SigSpec> port(lut, \I1, 0) === port(carry, \I0, 0)
	index <SigSpec> port(lut, \I2, 0) === port(carry, \I1, 0)
endmatch

code
	accept;
endcode
