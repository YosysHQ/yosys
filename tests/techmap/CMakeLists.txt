# add_test(NAME recursive_runtest
#         COMMAND $<TARGET_FILE:yosys_exe>)


# cf tests/techmap/run-test.mk (generated by run-test.sh)

add_test(NAME abc9.ys
    COMMAND $<TARGET_FILE:yosys_exe> -ql abc9.log -e "select out of bounds" ${CMAKE_CURRENT_SOURCE_DIR}/abc9.ys
)
add_test(NAME adff2dff.ys
    COMMAND $<TARGET_FILE:yosys_exe> -ql adff2dff.log -e "select out of bounds" ${CMAKE_CURRENT_SOURCE_DIR}/adff2dff.ys
)
add_test(NAME aigmap.ys
    COMMAND $<TARGET_FILE:yosys_exe> -ql abc9.log -e "select out of bounds" ${CMAKE_CURRENT_SOURCE_DIR}/aigmap.ys
)
add_test(NAME autopurge.ys
    COMMAND $<TARGET_FILE:yosys_exe> -ql abc9.log -e "select out of bounds" ${CMAKE_CURRENT_SOURCE_DIR}/autopurge.ys
)

# add_test(NAME bug2183.ys
# bug2183.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql bug2183.log -e "select out of bounds" bug2183.ys
#     @echo 'Passed bug2183.ys'
# ) bug2321.ys
# add_test(NAME bug2321.ys
# bug2321.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql bug2321.log -e "select out of bounds" bug2321.ys
#     @echo 'Passed bug2321.ys'
# ) bug2332.ys
# add_test(NAME bug2332.ys
# bug2332.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql bug2332.log -e "select out of bounds" bug2332.ys
#     @echo 'Passed bug2332.ys'
# ) bug2759.ys
# add_test(NAME bug2759.ys
# bug2759.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql bug2759.log -e "select out of bounds" bug2759.ys
#     @echo 'Passed bug2759.ys'
# ) bug2972.ys
# add_test(NAME bug2972.ys
# bug2972.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql bug2972.log -e "select out of bounds" bug2972.ys
#     @echo 'Passed bug2972.ys'
# ) cellname.ys
# add_test(NAME cellname.ys
# cellname.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql cellname.log -e "select out of bounds" cellname.ys
#     @echo 'Passed cellname.ys'
# ) clkbufmap.ys
# add_test(NAME clkbufmap.ys
# clkbufmap.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql clkbufmap.log -e "select out of bounds" clkbufmap.ys
#     @echo 'Passed clkbufmap.ys'
# ) cmp2lcu.ys
# add_test(NAME cmp2lcu.ys
# cmp2lcu.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql cmp2lcu.log -e "select out of bounds" cmp2lcu.ys
#     @echo 'Passed cmp2lcu.ys'
# ) dff2ff.ys
# add_test(NAME dff2ff.ys
# dff2ff.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dff2ff.log -e "select out of bounds" dff2ff.ys
#     @echo 'Passed dff2ff.ys'
# ) dffinit.ys
# add_test(NAME dffinit.ys
# dffinit.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dffinit.log -e "select out of bounds" dffinit.ys
#     @echo 'Passed dffinit.ys'
# ) dfflegalize_adff.ys
# add_test(NAME dfflegalize_adff.ys
# dfflegalize_adff.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_adff.log -e "select out of bounds" dfflegalize_adff.ys
#     @echo 'Passed dfflegalize_adff.ys'
# ) dfflegalize_adff_init.ys
# add_test(NAME dfflegalize_adff_init.ys
# dfflegalize_adff_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_adff_init.log -e "select out of bounds" dfflegalize_adff_init.ys
#     @echo 'Passed dfflegalize_adff_init.ys'
# ) dfflegalize_adlatch.ys
# add_test(NAME dfflegalize_adlatch.ys
# dfflegalize_adlatch.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_adlatch.log -e "select out of bounds" dfflegalize_adlatch.ys
#     @echo 'Passed dfflegalize_adlatch.ys'
# ) dfflegalize_adlatch_init.ys
# add_test(NAME dfflegalize_adlatch_init.ys
# dfflegalize_adlatch_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_adlatch_init.log -e "select out of bounds" dfflegalize_adlatch_init.ys
#     @echo 'Passed dfflegalize_adlatch_init.ys'
# ) dfflegalize_aldff.ys
# add_test(NAME dfflegalize_aldff.ys
# dfflegalize_aldff.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_aldff.log -e "select out of bounds" dfflegalize_aldff.ys
#     @echo 'Passed dfflegalize_aldff.ys'
# ) dfflegalize_aldff_init.ys
# add_test(NAME dfflegalize_aldff_init.ys
# dfflegalize_aldff_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_aldff_init.log -e "select out of bounds" dfflegalize_aldff_init.ys
#     @echo 'Passed dfflegalize_aldff_init.ys'
# ) dfflegalize_dff.ys
# add_test(NAME dfflegalize_dff.ys
# dfflegalize_dff.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dff.log -e "select out of bounds" dfflegalize_dff.ys
#     @echo 'Passed dfflegalize_dff.ys'
# ) dfflegalize_dff_init.ys
# add_test(NAME dfflegalize_dff_init.ys
# dfflegalize_dff_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dff_init.log -e "select out of bounds" dfflegalize_dff_init.ys
#     @echo 'Passed dfflegalize_dff_init.ys'
# ) dfflegalize_dffsr.ys
# add_test(NAME dfflegalize_dffsr.ys
# dfflegalize_dffsr.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dffsr.log -e "select out of bounds" dfflegalize_dffsr.ys
#     @echo 'Passed dfflegalize_dffsr.ys'
# ) dfflegalize_dffsr_init.ys
# add_test(NAME dfflegalize_dffsr_init.ys
# dfflegalize_dffsr_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dffsr_init.log -e "select out of bounds" dfflegalize_dffsr_init.ys
#     @echo 'Passed dfflegalize_dffsr_init.ys'
# ) dfflegalize_dlatch.ys
# add_test(NAME dfflegalize_dlatch.ys
# dfflegalize_dlatch.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dlatch.log -e "select out of bounds" dfflegalize_dlatch.ys
#     @echo 'Passed dfflegalize_dlatch.ys'
# ) dfflegalize_dlatch_const.ys
# add_test(NAME dfflegalize_dlatch_const.ys
# dfflegalize_dlatch_const.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dlatch_const.log -e "select out of bounds" dfflegalize_dlatch_const.ys
#     @echo 'Passed dfflegalize_dlatch_const.ys'
# ) dfflegalize_dlatch_init.ys
# add_test(NAME dfflegalize_dlatch_init.ys
# dfflegalize_dlatch_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dlatch_init.log -e "select out of bounds" dfflegalize_dlatch_init.ys
#     @echo 'Passed dfflegalize_dlatch_init.ys'
# ) dfflegalize_dlatchsr.ys
# add_test(NAME dfflegalize_dlatchsr.ys
# dfflegalize_dlatchsr.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dlatchsr.log -e "select out of bounds" dfflegalize_dlatchsr.ys
#     @echo 'Passed dfflegalize_dlatchsr.ys'
# ) dfflegalize_dlatchsr_init.ys
# add_test(NAME dfflegalize_dlatchsr_init.ys
# dfflegalize_dlatchsr_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_dlatchsr_init.log -e "select out of bounds" dfflegalize_dlatchsr_init.ys
#     @echo 'Passed dfflegalize_dlatchsr_init.ys'
# ) dfflegalize_inv.ys
# add_test(NAME dfflegalize_inv.ys
# dfflegalize_inv.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_inv.log -e "select out of bounds" dfflegalize_inv.ys
#     @echo 'Passed dfflegalize_inv.ys'
# ) dfflegalize_mince.ys
# add_test(NAME dfflegalize_mince.ys
# dfflegalize_mince.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_mince.log -e "select out of bounds" dfflegalize_mince.ys
#     @echo 'Passed dfflegalize_mince.ys'
# ) dfflegalize_minsrst.ys
# add_test(NAME dfflegalize_minsrst.ys
# dfflegalize_minsrst.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_minsrst.log -e "select out of bounds" dfflegalize_minsrst.ys
#     @echo 'Passed dfflegalize_minsrst.ys'
# ) dfflegalize_sr.ys
# add_test(NAME dfflegalize_sr.ys
# dfflegalize_sr.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_sr.log -e "select out of bounds" dfflegalize_sr.ys
#     @echo 'Passed dfflegalize_sr.ys'
# ) dfflegalize_sr_init.ys
# add_test(NAME dfflegalize_sr_init.ys
# dfflegalize_sr_init.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflegalize_sr_init.log -e "select out of bounds" dfflegalize_sr_init.ys
#     @echo 'Passed dfflegalize_sr_init.ys'
# ) dfflibmap.ys
# add_test(NAME dfflibmap.ys
# dfflibmap.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dfflibmap.log -e "select out of bounds" dfflibmap.ys
#     @echo 'Passed dfflibmap.ys'
# ) dffunmap.ys
# add_test(NAME dffunmap.ys
# dffunmap.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql dffunmap.log -e "select out of bounds" dffunmap.ys
#     @echo 'Passed dffunmap.ys'
# ) extractinv.ys
# add_test(NAME extractinv.ys
# extractinv.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql extractinv.log -e "select out of bounds" extractinv.ys
#     @echo 'Passed extractinv.ys'
# ) iopadmap.ys
# add_test(NAME iopadmap.ys
# iopadmap.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql iopadmap.log -e "select out of bounds" iopadmap.ys
#     @echo 'Passed iopadmap.ys'
# ) pmux2mux.ys
# add_test(NAME pmux2mux.ys
# pmux2mux.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql pmux2mux.log -e "select out of bounds" pmux2mux.ys
#     @echo 'Passed pmux2mux.ys'
# ) shiftx2mux.ys
# add_test(NAME shiftx2mux.ys
# shiftx2mux.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql shiftx2mux.log -e "select out of bounds" shiftx2mux.ys
#     @echo 'Passed shiftx2mux.ys'
# ) techmap_replace.ys
# add_test(NAME techmap_replace.ys
# techmap_replace.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql techmap_replace.log -e "select out of bounds" techmap_replace.ys
#     @echo 'Passed techmap_replace.ys'
# ) wireinit.ys
# add_test(NAME wireinit.ys
# wireinit.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql wireinit.log -e "select out of bounds" wireinit.ys
#     @echo 'Passed wireinit.ys'
# ) zinit.ys
# add_test(NAME zinit.ys
# zinit.ys:
#     COMMAND $<TARGET_FILE:yosys_exe> -ql zinit.log -e "select out of bounds" zinit.ys
#     @echo 'Passed zinit.ys'
# ) mem_simple_4x1_runtest.sh
# add_test(NAME mem_simple_4x1_runtest.sh
# mem_simple_4x1_runtest.sh:
#     @bash -v mem_simple_4x1_runtest.sh >mem_simple_4x1_runtest.log 2>&1
#     @echo 'Passed mem_simple_4x1_runtest.sh'
# ) recursive_runtest.sh
# add_test(NAME recursive_runtest.sh
# recursive_runtest.sh:
#     @bash -v recursive_runtest.sh >recursive_runtest.log 2>&1
#     @echo 'Passed recursive_runtest.sh'
